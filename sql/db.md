

# 📘 Описание структуры базы данных проекта

Этот документ — **шпаргалка на соревнованиях**.
Он объясняет:
- какие таблицы есть в базе данных;
- за что отвечает каждая таблица;
- что означает каждое поле;
- какие таблицы заполняются вручную/скриптами, а какие считаются автоматически.

Документ можно держать открытым во время работы, чтобы **не путаться в логике БД**.

---

## 🧱 Общая логика БД (важно понять один раз)

Архитектура построена по принципу:

1. **Источники данных** → откуда пришли файлы
2. **Треки** → логические маршруты / рейсы
3. **Точки треков** → координаты и телеметрия
4. **Признаки треков** → агрегаты для анализа и ML
5. **Контекст** → внешние данные (погода и т.п.)
6. **Аналитика / ML** → кластеры и предсказания
7. **VIEW** → удобные витрины для дашборда

---

# 1️⃣ Таблицы хранения данных

## 🗂️ `data_sources`
**Назначение:**
Хранит информацию об источниках данных (файлы GPX / parquet / csv / API).
Используется для:
- отслеживания загруженных файлов;
- защиты от повторной загрузки (дубликатов);
- аудита источников данных.

**Поля:**
- `source_id` — уникальный идентификатор источника
- `source_type` — тип источника (`gpx`, `parquet`, `csv`, `api`)
- `source_name` — имя файла или логическое имя набора данных
- `source_path` — путь к файлу или URL (если есть)
- `file_hash` — хэш файла (используется для защиты от дублей)
- `received_at` — дата и время загрузки
- `extra` — дополнительные метаданные (JSON)

📌 **Заполняется:** ingest-скриптом при загрузке файлов

---

## 🧭 `tracks`
**Назначение:**
Хранит логические треки / маршруты / рейсы.
Один трек = один маршрут движения (например один GPX-трек или один рейс самосвала).

**Поля:**
- `track_id` — уникальный идентификатор трека
- `source_id` — ссылка на `data_sources`
- `ext_track_id` — внешний id (например `tripid` или имя трека в GPX)
- `ext_object_id` — идентификатор объекта (машина, самосвал, устройство)
- `ext_driver_id` — идентификатор водителя / пользователя (если есть)
- `track_name` — человекочитаемое имя трека
- `track_type` — тип трека (`hike`, `bike`, `truck_trip` и т.п.)
- `start_time` — время начала трека
- `end_time` — время окончания трека
- `segment_count` — количество сегментов (актуально для GPX)
- `point_count` — количество точек в треке
- `min_lat`, `min_lon`, `max_lat`, `max_lon` — bounding box трека
- `created_at` — время создания записи
- `extra` — дополнительные метаданные (JSON)

📌 **Заполняется:** ingest-скриптом (один раз на трек)

---

## 📍 `track_points`
**Назначение:**
Хранит **сырые точки трека** — координаты и телеметрию.
Это самая большая и важная таблица.

**Поля:**
- `point_id` — уникальный идентификатор точки
- `track_id` — ссылка на `tracks`
- `seq` — порядковый номер точки в треке
- `segment_index` — номер сегмента (для GPX, обычно 0)
- `lat`, `lon` — широта и долгота
- `x`, `y` — координаты в плоской системе (если есть)
- `ele` — высота
- `time` — время точки
- `speed` — скорость
- `heading` — направление движения
- `extra` — любые дополнительные датчики (JSON)

📌 **Заполняется:** ingest-скриптом (много строк на один трек)

---

## 📊 `track_features`
**Назначение:**
Хранит агрегированные признаки трека.
Используется для:
- EDA;
- кластеризации;
- обучения моделей.

**Поля:**
- `track_id` — ссылка на `tracks`
- `distance_m` — длина трека (метры)
- `duration_s` — длительность (секунды)
- `elev_min_m`, `elev_max_m` — мин/макс высота
- `elev_gain_m`, `elev_loss_m` — набор/сброс высоты
- `avg_speed_mps` — средняя скорость
- `max_speed_mps` — максимальная скорость
- `stop_time_s` — время стоянок
- `stop_ratio` — доля стоянок
- `point_density_per_km` — плотность точек
- `updated_at` — время пересчёта
- `extra` — дополнительные признаки (JSON)

📌 **Заполняется:** аналитическим скриптом после загрузки точек

---

## 🌦️ `context_time_series`
**Назначение:**
Хранит внешние временные данные (например погоду).

**Поля:**
- `context_id` — идентификатор записи
- `source_id` — ссылка на `data_sources`
- `time` — временная метка
- `lat`, `lon` — координаты (если применимо)
- `values` — значения показателей (JSON)

📌 **Заполняется:** ingest-скриптом для контекстных данных

---

# 2️⃣ Аналитика и машинное обучение

## 🧩 `cluster_runs`
**Назначение:**
Хранит информацию о запусках кластеризации.

**Поля:**
- `cluster_run_id` — идентификатор запуска
- `run_tag` — имя запуска (`kmeans_v1`, `dbscan_eps_0.3`)
- `method` — метод кластеризации
- `created_at` — время запуска
- `silhouette`, `calinski_harabasz`, `davies_bouldin` — метрики качества
- `params` — параметры алгоритма (JSON)

---

## 🏷️ `track_cluster_labels`
**Назначение:**
Связывает треки с кластерами для конкретного запуска.

**Поля:**
- `cluster_run_id` — ссылка на `cluster_runs`
- `track_id` — ссылка на `tracks`
- `cluster_id` — номер кластера

---

## 🤖 `model_runs`
**Назначение:**
Хранит информацию о запусках ML-моделей.

**Поля:**
- `model_run_id` — идентификатор запуска
- `run_tag` — имя модели
- `task_type` — тип задачи (`regression`, `classification`)
- `target_name` — целевая переменная
- `created_at` — время обучения
- `metrics` — метрики качества (JSON)
- `params` — параметры модели (JSON)

---

## 🔮 `track_predictions`
**Назначение:**
Хранит предсказания моделей для треков.

**Поля:**
- `model_run_id` — ссылка на `model_runs`
- `track_id` — ссылка на `tracks`
- `y_pred_num` — числовое предсказание (регрессия)
- `y_pred_class` — класс (классификация)
- `y_pred_proba` — вероятности (JSON)

---

# 3️⃣ Представления (VIEW)

VIEW — это **витрины**, данные в них не хранятся.

- `v_tracks_overview` — главная витрина (1 строка = 1 трек)
- `v_track_points_map` — точки для карты
- `v_speed_by_hour` — средняя скорость по часам
- `v_track_distances` — дистанции и базовые признаки
- `v_cluster_run_quality` — качество кластеризации
- `v_cluster_distribution_latest` — распределение по кластерам
- `v_context_time_series` — контекстные данные

📌 **Используются:** дашбордом, EDA, демонстрацией экспертам

---

## ✅ Короткий итог
- Таблицы — хранят данные
- VIEW — показывают данные
- `extra` / `values` — защита от неожиданных полей
- Структура универсальна и подходит под разные входные данные

👉 Этот файл — твоя **карта БД на соревнованиях**.