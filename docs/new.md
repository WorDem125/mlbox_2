📘 Документация проекта

Система анализа рисков маршрутов с непрерывным обучением моделей

⸻

1. Общая идея проекта

В рамках проекта реализована система анализа участков маршрутов на основе геоданных.
Система решает две основные задачи:
	1.	Определение типа риска на участке маршрута:
	•	none — риск отсутствует
	•	flood — потенциальное затопление
	•	fire — потенциальная пожарная опасность
	2.	Оценка сложности эвакуации:
	•	easy
	•	medium
	•	hard

Проект построен по модульному принципу и включает:
	•	агент обучения моделей,
	•	REST API для инференса,
	•	клиентское приложение на Streamlit,
	•	механизм непрерывного обучения.

⸻

2. Архитектура системы (высокоуровнево)



Streamlit (UI)
     ↓ HTTP
FastAPI (API)
     ↓
ML-модели (joblib)
     ↓
База данных (PostgreSQL)
     ↑
Агент непрерывного обучения


Каждый компонент выполняет одну чёткую роль и не дублирует функциональность других.

⸻

3. Агент обучения моделей (Train Agent)

Назначение

Агент отвечает за:
	•	загрузку размеченных данных из базы данных;
	•	формирование обучающего датасета;
	•	обучение нескольких моделей классификации;
	•	сравнение качества моделей;
	•	сохранение лучших моделей и метаданных.

Используемые данные

Агент работает с уже подготовленной таблицей:

segment_labels

Таблица содержит:
	•	признаки сегмента маршрута,
	•	кластер,
	•	целевые метки risk_label и evac_label.

⚠️ Агент не изменяет данные и не создаёт временные таблицы.

⸻

Алгоритмы, используемые агентом

Для каждой задачи (risk / evac) перебираются минимум 3 алгоритма:
	•	Logistic Regression
	•	Random Forest
	•	Gradient Boosting

Для сравнения используется метрика:


F1_macro


Логика выбора и сохранения модели
	1.	Агент обучает все алгоритмы.
	2.	Сравнивает F1_macro.
	3.	Выбирает лучшую модель.
	4.	Сравнивает её с предыдущей сохранённой версией:
	•	если улучшение есть → модель сохраняется;
	•	если улучшения нет → модель не перезаписывается.

Таким образом реализован контроль качества и защита от деградации модели.

⸻

Что сохраняется

В папку models/ сохраняются:
	•	файл модели (.joblib);
	•	метаданные (.json), содержащие:
	•	метрики,
	•	параметры обучения,
	•	распределение классов,
	•	дату и время создания.

Также информация о запуске сохраняется в таблицу model_runs в базе данных.

⸻

Запуск агента


python -m src.agents.train_agent --task both



Возможные режимы:
	•	--task risk
	•	--task evac
	•	--task both

⸻

4. REST API (FastAPI)

Назначение

API — центральный компонент системы.
Он:
	•	загружает актуальные модели при старте;
	•	предоставляет интерфейс для предсказаний;
	•	взаимодействует с агентом непрерывного обучения;
	•	изолирует UI от ML-логики.

⸻

Основные эндпоинты


Метод
Endpoint
Назначение
GET
/health
Проверка состояния сервиса
GET
/models
Информация о загруженных моделях
POST
/predict
Получение предсказаний


Эндпоинт /predict

Принимает признаки сегмента (или координаты, преобразованные в признаки)
Возвращает:

{
  "risk_label": "flood",
  "evac_label": "medium"
}


Запуск API


python -m uvicorn src.api.api:app --host 0.0.0.0 --port 8000 --reload


5. Непрерывное обучение (Continuous Learning)

Идея

В системе реализован фоновый механизм, который:
	•	периодически проверяет изменения в данных;
	•	при необходимости переобучает модели;
	•	автоматически обновляет лучшие версии моделей.

⸻

Как это работает
	1.	Каждые N секунд (например, 30) API:
	•	считывает сигнатуру данных:
	•	количество строк,
	•	максимальный segment_id,
	•	максимальный created_at.
	2.	Сравнивает с предыдущей сигнатурой.
	3.	Если данные не изменились:
	•	обучение не запускается.
	4.	Если появились новые данные:
	•	вызывается агент обучения;
	•	новая модель сравнивается с текущей;
	•	при улучшении модель обновляется.

Все события логируются в консоль.

Пример лога:


[CL] tick=... | changed=False | models: risk=yes evac=yes

6. Клиентское приложение (Streamlit)

Назначение

Streamlit используется как демонстрационное клиентское приложение.

Оно:
	•	не работает напрямую с БД;
	•	не содержит ML-логики;
	•	взаимодействует только через API.

⸻

Функциональность приложения
	•	проверка состояния сервиса (/health);
	•	отображение информации о моделях (/models);
	•	ввод данных пользователем;
	•	получение и отображение предсказаний (/predict).

⸻

Запуск Streamlit

streamlit run app.py


8. Итог

В результате реализована полноценная ML-система, включающая:
	•	полуавтоматическую разметку данных;
	•	обучение и сравнение моделей;
	•	непрерывное обучение;
	•	REST API;
	•	клиентское приложение.

Проект демонстрирует полный жизненный цикл ML-решения:
от данных → до пользователя.

